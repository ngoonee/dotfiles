### Volume Control ###
#shift + super + q
    #xdotool keyup Super keyup Shift keyup q key XF86AudioRaiseVolume keydown Shift keydown Super
#shift + super + a
    #xdotool keyup Super keyup Shift keyup a key XF86AudioLowerVolume keydown Shift keydown Super
#shift + super + s
    #xdotool keyup Super keyup Shift keyup s key XF86AudioMute keydown Shift keydown Super
shift + super + v
    pavucontrol

### Music Player ###
#shift + super + {x,z,c}
    #xdotool key XF86Audio{Play,Prev,Next}
shift + super + x
    xdotool keyup Super keyup Shift keyup x key XF86AudioPlay keydown Shift keydown Super
shift + super + z
    xdotool keyup Super keyup Shift keyup z key XF86AudioPrev keydown Shift keydown Super
shift + super + c
    xdotool keyup Super keyup Shift keyup c key XF86AudioNext keydown Shift keydown Super

### Shortcuts ###
super + Return
    alacritty
super + d
    qdbus org.kde.plasmashell /PlasmaShell org.kde.PlasmaShell.activateLauncherMenu
    #rofi -show combi
super + g
    emacsclient -a '' -c -n -e '(switch-to-buffer nil)'

button21
    wacom
button23
    xdotool keydown Ctrl key minus keyup Ctrl
button24
    xdotool keydown Ctrl key plus keyup Ctrl
button25
    xdotool keydown Ctrl keydown Shift key p keyup Shift keyup Ctrl
button26
    xdotool keydown Ctrl keydown Shift key t keyup Shift keyup Ctrl
button27
    xdotool keydown Ctrl key z keyup Ctrl
button28
    xdotool keydown Ctrl key y keyup Ctrl
