#!/bin/sh

#ARCH=$(uname -m)
#if [ $ARCH == "x86_64" ]; then
#    schroot -p -- $0
#    exit 0
#fi

if [ "$(pidof pulseaudio)" ] ; then
    OSSCONVERTER=padsp
else
    OSSCONVERTER=aoss
fi

WINEDEBUG=-all WINEPREFIX=/home/LinuxProg/wine/utorrent $OSSCONVERTER wine "C:\Program Files\uTorrent\uTorrent.exe"

exit 0
