#!/bin/sh
WINEDIR="/home/data/Apps/wine64"
GAMEDIR="$WINEDIR/drive_c/Windemic 1.60"

cd "$GAMEDIR"

PREFIX="env WINEDEBUG=-all WINEPREFIX=$WINEDIR"
POSTFIX="wine Windemic.exe"

COMMAND2="$PREFIX $POSTFIX"

`$COMMAND2`
